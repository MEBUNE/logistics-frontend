# logistics-frontend
Rhema Website — a TypeScript React + Vite starter with an Express dev/prod server, reusable UI components, TanStack Query for data fetching, and Drizzle-ready schema. Fast to boot, easy to extend, and ready for DB integration or static deployment.
